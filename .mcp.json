{
  "mcpServers": {
    "alphaxiv": {
      "type": "sse",
      "url": "https://api.alphaxiv.org/mcp/v1",
      "note": "Academic paper search via alphaXiv. Used by research-agent (6 tools) and implement-agent (2 tools). Optional — falls back to WebSearch/WebFetch."
    },
    "context7": {
      "type": "sse",
      "url": "https://mcp.context7.com/mcp",
      "note": "Framework API documentation lookup. Used by research-agent and implement-agent. Optional — falls back to WebSearch."
    }
  }
}

model:
  type: classifier
  base_channels: 64
  num_blocks: 2
  num_classes: 10

training:
  lr: 0.001
  batch_size: 16
  epochs: 100
  weight_decay: 0.01
  scheduler: cosine

data:
  train_dir: ./data/train
  val_dir: ./data/val
  image_size: 32
